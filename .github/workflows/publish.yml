on:
  workflow_dispatch:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

name: Build and Deploy to Netlify

permissions:
  contents: read

concurrency:
  group: "netlify-deploy"
  cancel-in-progress: true

jobs:

  build-1-pct-treaty-impact:
    name: "Build The 1% Treaty: Harnessing Greed to Eradicate Disea"
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Cache Quarto freeze
        uses: actions/cache@v4
        with:
          path: _freeze
          key: quarto-freeze-1-pct-treaty-impact-${{ hashFiles('**/*.qmd', '**/*.ipynb') }}

          restore-keys: |
            quarto-freeze-1-pct-treaty-impact-
            quarto-freeze-

      - name: Set up Quarto
        uses: quarto-dev/quarto-actions/setup@v2

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true

      - name: Install Graphviz
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y graphviz

      - name: Install Python dependencies
        run: uv pip install --system -r requirements.txt

      - name: Install dih_models package
        run: uv pip install --system -e .

      - name: Install Jupyter kernel
        run: |
          python -m ipykernel install --user --name dih-project-kernel --display-name "DIH Project"

      - name: Cache TinyTeX
        uses: actions/cache@v4
        with:
          path: ~/.TinyTeX
          key: tinytex-${{ runner.os }}-${{ hashFiles('.github/workflows/publish.yml') }}
          restore-keys: |
            tinytex-${{ runner.os }}-

      - name: Install LaTeX for PDF/EPUB
        run: |
          if [ ! -d ~/.TinyTeX ]; then
            quarto install tinytex
          fi
          ~/.TinyTeX/bin/*/tlmgr update --self || true
          ~/.TinyTeX/bin/*/tlmgr install fontspec unicode-math lualatex-math luaotfload || true

      - name: "Render Build The 1% Treaty: Harnessing Greed to Eradicate Disea"
        run: python scripts/render-quarto.py 1-pct-treaty-impact
        timeout-minutes: 30

      - name: Upload LaTeX logs on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: latex-logs-1-pct-treaty-impact
          path: |
            _build_temp/1-pct-treaty-impact/*.log
            _build_temp/1-pct-treaty-impact/**/*.log
            _build_temp/1-pct-treaty-impact/*.aux
            _build_temp/1-pct-treaty-impact/**/*.aux
            _build_temp/1-pct-treaty-impact/*.toc
            _build_temp/1-pct-treaty-impact/**/*.toc
            _build_temp/1-pct-treaty-impact/*.out
            _build_temp/1-pct-treaty-impact/**/*.out
          retention-days: 7


      - name: "Deploy Build The 1% Treaty: Harnessing Greed to Eradicate Disea to Netlify (immediate)"
        if: github.event_name == 'push'
        uses: nwtgck/actions-netlify@v3.0
        with:
          publish-dir: '_build_temp/1-pct-treaty-impact/_site/1-pct-treaty-impact'
          production-branch: master
          github-token: ${{ secrets.GITHUB_TOKEN }}

          deploy-message: "Deploy 1-pct-treaty-impact site from GitHub Actions"
          enable-pull-request-comment: true
          enable-commit-comment: true
          overwrites-pull-request-comment: true
        env:
          NETLIFY_AUTH_TOKEN: ${{ secrets.NETLIFY_AUTH_TOKEN }}

          NETLIFY_SITE_ID: 8cb62a4c-0a10-4a92-a9ab-088c5b2cf094

        timeout-minutes: 1





  build-cost-of-change:
    name: "Build The Price of Political Change: A Cost-Benefit Fram"
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Cache Quarto freeze
        uses: actions/cache@v4
        with:
          path: _freeze
          key: quarto-freeze-cost-of-change-${{ hashFiles('**/*.qmd', '**/*.ipynb') }}

          restore-keys: |
            quarto-freeze-cost-of-change-
            quarto-freeze-

      - name: Set up Quarto
        uses: quarto-dev/quarto-actions/setup@v2

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true

      - name: Install Graphviz
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y graphviz

      - name: Install Python dependencies
        run: uv pip install --system -r requirements.txt

      - name: Install dih_models package
        run: uv pip install --system -e .

      - name: Install Jupyter kernel
        run: |
          python -m ipykernel install --user --name dih-project-kernel --display-name "DIH Project"

      - name: Cache TinyTeX
        uses: actions/cache@v4
        with:
          path: ~/.TinyTeX
          key: tinytex-${{ runner.os }}-${{ hashFiles('.github/workflows/publish.yml') }}
          restore-keys: |
            tinytex-${{ runner.os }}-

      - name: Install LaTeX
        run: |
          if [ ! -d ~/.TinyTeX ]; then
            quarto install tinytex
          fi
          ~/.TinyTeX/bin/*/tlmgr update --self || true
          ~/.TinyTeX/bin/*/tlmgr install fontspec unicode-math lualatex-math luaotfload || true

      - name: "Render Build The Price of Political Change: A Cost-Benefit Fram"
        run: python scripts/render-quarto.py cost-of-change
        timeout-minutes: 30

      - name: Upload LaTeX logs on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: latex-logs-cost-of-change
          path: |
            _build_temp/cost-of-change/*.log
            _build_temp/cost-of-change/**/*.log
            _build_temp/cost-of-change/*.aux
            _build_temp/cost-of-change/**/*.aux
            _build_temp/cost-of-change/*.toc
            _build_temp/cost-of-change/**/*.toc
            _build_temp/cost-of-change/*.out
            _build_temp/cost-of-change/**/*.out
          retention-days: 7


      - name: "Deploy Build The Price of Political Change: A Cost-Benefit Fram to Netlify (immediate)"
        if: github.event_name == 'push'
        uses: nwtgck/actions-netlify@v3.0
        with:
          publish-dir: '_build_temp/cost-of-change/_site/cost-of-change'
          production-branch: master
          github-token: ${{ secrets.GITHUB_TOKEN }}

          deploy-message: "Deploy cost-of-change site from GitHub Actions"
          enable-pull-request-comment: true
          enable-commit-comment: true
          overwrites-pull-request-comment: true
        env:
          NETLIFY_AUTH_TOKEN: ${{ secrets.NETLIFY_AUTH_TOKEN }}

          NETLIFY_SITE_ID: 7daf95c9-506f-4cb9-99e3-2f0f9477f816

        timeout-minutes: 1





  build-dfda-impact:
    name: "Build How to Prevent a Year of Death and Suffering for 8"
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Cache Quarto freeze
        uses: actions/cache@v4
        with:
          path: _freeze
          key: quarto-freeze-dfda-impact-${{ hashFiles('**/*.qmd', '**/*.ipynb') }}

          restore-keys: |
            quarto-freeze-dfda-impact-
            quarto-freeze-

      - name: Set up Quarto
        uses: quarto-dev/quarto-actions/setup@v2

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true

      - name: Install Graphviz
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y graphviz

      - name: Install Python dependencies
        run: uv pip install --system -r requirements.txt

      - name: Install dih_models package
        run: uv pip install --system -e .

      - name: Install Jupyter kernel
        run: |
          python -m ipykernel install --user --name dih-project-kernel --display-name "DIH Project"

      - name: Cache TinyTeX
        uses: actions/cache@v4
        with:
          path: ~/.TinyTeX
          key: tinytex-${{ runner.os }}-${{ hashFiles('.github/workflows/publish.yml') }}
          restore-keys: |
            tinytex-${{ runner.os }}-

      - name: Install LaTeX
        run: |
          if [ ! -d ~/.TinyTeX ]; then
            quarto install tinytex
          fi
          ~/.TinyTeX/bin/*/tlmgr update --self || true
          ~/.TinyTeX/bin/*/tlmgr install fontspec unicode-math lualatex-math luaotfload || true

      - name: "Render Build How to Prevent a Year of Death and Suffering for 8"
        run: python scripts/render-quarto.py dfda-impact
        timeout-minutes: 30

      - name: Upload LaTeX logs on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: latex-logs-dfda-impact
          path: |
            _build_temp/dfda-impact/*.log
            _build_temp/dfda-impact/**/*.log
            _build_temp/dfda-impact/*.aux
            _build_temp/dfda-impact/**/*.aux
            _build_temp/dfda-impact/*.toc
            _build_temp/dfda-impact/**/*.toc
            _build_temp/dfda-impact/*.out
            _build_temp/dfda-impact/**/*.out
          retention-days: 7


      - name: "Deploy Build How to Prevent a Year of Death and Suffering for 8 to Netlify (immediate)"
        if: github.event_name == 'push'
        uses: nwtgck/actions-netlify@v3.0
        with:
          publish-dir: '_build_temp/dfda-impact/_site/dfda-impact'
          production-branch: master
          github-token: ${{ secrets.GITHUB_TOKEN }}

          deploy-message: "Deploy dfda-impact site from GitHub Actions"
          enable-pull-request-comment: true
          enable-commit-comment: true
          overwrites-pull-request-comment: true
        env:
          NETLIFY_AUTH_TOKEN: ${{ secrets.NETLIFY_AUTH_TOKEN }}

          NETLIFY_SITE_ID: a314a33e-3155-4067-8624-706266899419

        timeout-minutes: 1





  build-dfda-spec:
    name: "Build dFDA: A Decentralized Framework for Drug Assessmen"
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Cache Quarto freeze
        uses: actions/cache@v4
        with:
          path: _freeze
          key: quarto-freeze-dfda-spec-${{ hashFiles('**/*.qmd', '**/*.ipynb') }}

          restore-keys: |
            quarto-freeze-dfda-spec-
            quarto-freeze-

      - name: Set up Quarto
        uses: quarto-dev/quarto-actions/setup@v2

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true

      - name: Install Graphviz
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y graphviz

      - name: Install Python dependencies
        run: uv pip install --system -r requirements.txt

      - name: Install dih_models package
        run: uv pip install --system -e .

      - name: Install Jupyter kernel
        run: |
          python -m ipykernel install --user --name dih-project-kernel --display-name "DIH Project"

      - name: Cache TinyTeX
        uses: actions/cache@v4
        with:
          path: ~/.TinyTeX
          key: tinytex-${{ runner.os }}-${{ hashFiles('.github/workflows/publish.yml') }}
          restore-keys: |
            tinytex-${{ runner.os }}-

      - name: Install LaTeX
        run: |
          if [ ! -d ~/.TinyTeX ]; then
            quarto install tinytex
          fi
          ~/.TinyTeX/bin/*/tlmgr update --self || true
          ~/.TinyTeX/bin/*/tlmgr install fontspec unicode-math lualatex-math luaotfload || true

      - name: "Render Build dFDA: A Decentralized Framework for Drug Assessmen"
        run: python scripts/render-quarto.py dfda-spec
        timeout-minutes: 30

      - name: Upload LaTeX logs on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: latex-logs-dfda-spec
          path: |
            _build_temp/dfda-spec/*.log
            _build_temp/dfda-spec/**/*.log
            _build_temp/dfda-spec/*.aux
            _build_temp/dfda-spec/**/*.aux
            _build_temp/dfda-spec/*.toc
            _build_temp/dfda-spec/**/*.toc
            _build_temp/dfda-spec/*.out
            _build_temp/dfda-spec/**/*.out
          retention-days: 7


      - name: "Deploy Build dFDA: A Decentralized Framework for Drug Assessmen to Netlify (immediate)"
        if: github.event_name == 'push'
        uses: nwtgck/actions-netlify@v3.0
        with:
          publish-dir: '_build_temp/dfda-spec/_site/dfda-spec'
          production-branch: master
          github-token: ${{ secrets.GITHUB_TOKEN }}

          deploy-message: "Deploy dfda-spec site from GitHub Actions"
          enable-pull-request-comment: true
          enable-commit-comment: true
          overwrites-pull-request-comment: true
        env:
          NETLIFY_AUTH_TOKEN: ${{ secrets.NETLIFY_AUTH_TOKEN }}

          NETLIFY_SITE_ID: 01e9af64-b6d0-48ee-8c3d-43994745ac3f

        timeout-minutes: 1





  build-federal-efficiency-audit:
    name: "Build Federal Resource Allocation Efficiency Audit"
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Cache Quarto freeze
        uses: actions/cache@v4
        with:
          path: _freeze
          key: quarto-freeze-federal-efficiency-audit-${{ hashFiles('**/*.qmd', '**/*.ipynb') }}

          restore-keys: |
            quarto-freeze-federal-efficiency-audit-
            quarto-freeze-

      - name: Set up Quarto
        uses: quarto-dev/quarto-actions/setup@v2

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true

      - name: Install Graphviz
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y graphviz

      - name: Install Python dependencies
        run: uv pip install --system -r requirements.txt

      - name: Install dih_models package
        run: uv pip install --system -e .

      - name: Install Jupyter kernel
        run: |
          python -m ipykernel install --user --name dih-project-kernel --display-name "DIH Project"

      - name: Cache TinyTeX
        uses: actions/cache@v4
        with:
          path: ~/.TinyTeX
          key: tinytex-${{ runner.os }}-${{ hashFiles('.github/workflows/publish.yml') }}
          restore-keys: |
            tinytex-${{ runner.os }}-

      - name: Install LaTeX
        run: |
          if [ ! -d ~/.TinyTeX ]; then
            quarto install tinytex
          fi
          ~/.TinyTeX/bin/*/tlmgr update --self || true
          ~/.TinyTeX/bin/*/tlmgr install fontspec unicode-math lualatex-math luaotfload || true

      - name: "Render Build Federal Resource Allocation Efficiency Audit"
        run: python scripts/render-quarto.py federal-efficiency-audit
        timeout-minutes: 30

      - name: Upload LaTeX logs on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: latex-logs-federal-efficiency-audit
          path: |
            _build_temp/federal-efficiency-audit/*.log
            _build_temp/federal-efficiency-audit/**/*.log
            _build_temp/federal-efficiency-audit/*.aux
            _build_temp/federal-efficiency-audit/**/*.aux
            _build_temp/federal-efficiency-audit/*.toc
            _build_temp/federal-efficiency-audit/**/*.toc
            _build_temp/federal-efficiency-audit/*.out
            _build_temp/federal-efficiency-audit/**/*.out
          retention-days: 7


      - name: "Deploy Build Federal Resource Allocation Efficiency Audit to Netlify (immediate)"
        if: github.event_name == 'push'
        uses: nwtgck/actions-netlify@v3.0
        with:
          publish-dir: '_build_temp/federal-efficiency-audit/_site/federal-efficiency-audit'
          production-branch: master
          github-token: ${{ secrets.GITHUB_TOKEN }}

          deploy-message: "Deploy federal-efficiency-audit site from GitHub Actions"
          enable-pull-request-comment: true
          enable-commit-comment: true
          overwrites-pull-request-comment: true
        env:
          NETLIFY_AUTH_TOKEN: ${{ secrets.NETLIFY_AUTH_TOKEN }}

          NETLIFY_SITE_ID: 9a8ec013-54cd-4b7a-9364-fa13dc46bd92

        timeout-minutes: 1





  build-iab:
    name: "Build Incentive Alignment Bonds: Making Public Goods Fin"
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Cache Quarto freeze
        uses: actions/cache@v4
        with:
          path: _freeze
          key: quarto-freeze-iab-${{ hashFiles('**/*.qmd', '**/*.ipynb') }}

          restore-keys: |
            quarto-freeze-iab-
            quarto-freeze-

      - name: Set up Quarto
        uses: quarto-dev/quarto-actions/setup@v2

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true

      - name: Install Graphviz
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y graphviz

      - name: Install Python dependencies
        run: uv pip install --system -r requirements.txt

      - name: Install dih_models package
        run: uv pip install --system -e .

      - name: Install Jupyter kernel
        run: |
          python -m ipykernel install --user --name dih-project-kernel --display-name "DIH Project"

      - name: Cache TinyTeX
        uses: actions/cache@v4
        with:
          path: ~/.TinyTeX
          key: tinytex-${{ runner.os }}-${{ hashFiles('.github/workflows/publish.yml') }}
          restore-keys: |
            tinytex-${{ runner.os }}-

      - name: Install LaTeX
        run: |
          if [ ! -d ~/.TinyTeX ]; then
            quarto install tinytex
          fi
          ~/.TinyTeX/bin/*/tlmgr update --self || true
          ~/.TinyTeX/bin/*/tlmgr install fontspec unicode-math lualatex-math luaotfload || true

      - name: "Render Build Incentive Alignment Bonds: Making Public Goods Fin"
        run: python scripts/render-quarto.py iab
        timeout-minutes: 30

      - name: Upload LaTeX logs on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: latex-logs-iab
          path: |
            _build_temp/iab/*.log
            _build_temp/iab/**/*.log
            _build_temp/iab/*.aux
            _build_temp/iab/**/*.aux
            _build_temp/iab/*.toc
            _build_temp/iab/**/*.toc
            _build_temp/iab/*.out
            _build_temp/iab/**/*.out
          retention-days: 7


      - name: "Deploy Build Incentive Alignment Bonds: Making Public Goods Fin to Netlify (immediate)"
        if: github.event_name == 'push'
        uses: nwtgck/actions-netlify@v3.0
        with:
          publish-dir: '_build_temp/iab/_site/iab'
          production-branch: master
          github-token: ${{ secrets.GITHUB_TOKEN }}

          deploy-message: "Deploy iab site from GitHub Actions"
          enable-pull-request-comment: true
          enable-commit-comment: true
          overwrites-pull-request-comment: true
        env:
          NETLIFY_AUTH_TOKEN: ${{ secrets.NETLIFY_AUTH_TOKEN }}

          NETLIFY_SITE_ID: 8f7611db-1075-48a2-abd8-7f77f55538f9

        timeout-minutes: 1





  build-invisible-graveyard:
    name: "Build The Invisible Graveyard: Quantifying the Mortality"
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Cache Quarto freeze
        uses: actions/cache@v4
        with:
          path: _freeze
          key: quarto-freeze-invisible-graveyard-${{ hashFiles('**/*.qmd', '**/*.ipynb') }}

          restore-keys: |
            quarto-freeze-invisible-graveyard-
            quarto-freeze-

      - name: Set up Quarto
        uses: quarto-dev/quarto-actions/setup@v2

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true

      - name: Install Graphviz
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y graphviz

      - name: Install Python dependencies
        run: uv pip install --system -r requirements.txt

      - name: Install dih_models package
        run: uv pip install --system -e .

      - name: Install Jupyter kernel
        run: |
          python -m ipykernel install --user --name dih-project-kernel --display-name "DIH Project"

      - name: Cache TinyTeX
        uses: actions/cache@v4
        with:
          path: ~/.TinyTeX
          key: tinytex-${{ runner.os }}-${{ hashFiles('.github/workflows/publish.yml') }}
          restore-keys: |
            tinytex-${{ runner.os }}-

      - name: Install LaTeX
        run: |
          if [ ! -d ~/.TinyTeX ]; then
            quarto install tinytex
          fi
          ~/.TinyTeX/bin/*/tlmgr update --self || true
          ~/.TinyTeX/bin/*/tlmgr install fontspec unicode-math lualatex-math luaotfload || true

      - name: "Render Build The Invisible Graveyard: Quantifying the Mortality"
        run: python scripts/render-quarto.py invisible-graveyard
        timeout-minutes: 30

      - name: Upload LaTeX logs on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: latex-logs-invisible-graveyard
          path: |
            _build_temp/invisible-graveyard/*.log
            _build_temp/invisible-graveyard/**/*.log
            _build_temp/invisible-graveyard/*.aux
            _build_temp/invisible-graveyard/**/*.aux
            _build_temp/invisible-graveyard/*.toc
            _build_temp/invisible-graveyard/**/*.toc
            _build_temp/invisible-graveyard/*.out
            _build_temp/invisible-graveyard/**/*.out
          retention-days: 7


      - name: "Deploy Build The Invisible Graveyard: Quantifying the Mortality to Netlify (immediate)"
        if: github.event_name == 'push'
        uses: nwtgck/actions-netlify@v3.0
        with:
          publish-dir: '_build_temp/invisible-graveyard/_site/invisible-graveyard'
          production-branch: master
          github-token: ${{ secrets.GITHUB_TOKEN }}

          deploy-message: "Deploy invisible-graveyard site from GitHub Actions"
          enable-pull-request-comment: true
          enable-commit-comment: true
          overwrites-pull-request-comment: true
        env:
          NETLIFY_AUTH_TOKEN: ${{ secrets.NETLIFY_AUTH_TOKEN }}

          NETLIFY_SITE_ID: ef37ca63-13e5-4403-bf47-173763af79ab

        timeout-minutes: 1





  build-manual:
    name: "Build How to End War and Disease"
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Cache Quarto freeze
        uses: actions/cache@v4
        with:
          path: _freeze
          key: quarto-freeze-manual-${{ hashFiles('**/*.qmd', '**/*.ipynb') }}

          restore-keys: |
            quarto-freeze-manual-
            quarto-freeze-

      - name: Set up Quarto
        uses: quarto-dev/quarto-actions/setup@v2

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true

      - name: Install Graphviz
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y graphviz

      - name: Install Python dependencies
        run: uv pip install --system -r requirements.txt

      - name: Install dih_models package
        run: uv pip install --system -e .

      - name: Install Jupyter kernel
        run: |
          python -m ipykernel install --user --name dih-project-kernel --display-name "DIH Project"

      - name: Cache TinyTeX
        uses: actions/cache@v4
        with:
          path: ~/.TinyTeX
          key: tinytex-${{ runner.os }}-${{ hashFiles('.github/workflows/publish.yml') }}
          restore-keys: |
            tinytex-${{ runner.os }}-

      - name: Install LaTeX for PDF/EPUB
        run: |
          if [ ! -d ~/.TinyTeX ]; then
            quarto install tinytex
          fi
          ~/.TinyTeX/bin/*/tlmgr update --self || true
          ~/.TinyTeX/bin/*/tlmgr install fontspec unicode-math lualatex-math luaotfload || true

      - name: "Render Build How to End War and Disease"
        run: python scripts/render-quarto.py manual
        timeout-minutes: 75

      - name: Upload LaTeX logs on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: latex-logs-manual
          path: |
            _build_temp/manual/*.log
            _build_temp/manual/**/*.log
            _build_temp/manual/*.aux
            _build_temp/manual/**/*.aux
            _build_temp/manual/*.toc
            _build_temp/manual/**/*.toc
            _build_temp/manual/*.out
            _build_temp/manual/**/*.out
          retention-days: 7


      - name: "Deploy Build How to End War and Disease to Netlify (immediate)"
        if: github.event_name == 'push'
        uses: nwtgck/actions-netlify@v3.0
        with:
          publish-dir: '_build_temp/manual/_manual/warondisease'
          production-branch: master
          github-token: ${{ secrets.GITHUB_TOKEN }}

          deploy-message: "Deploy manual site from GitHub Actions"
          enable-pull-request-comment: true
          enable-commit-comment: true
          overwrites-pull-request-comment: true
        env:
          NETLIFY_AUTH_TOKEN: ${{ secrets.NETLIFY_AUTH_TOKEN }}

          NETLIFY_SITE_ID: 4e36b0d6-9b80-49e2-bf16-eb4d2f79f062

        timeout-minutes: 1





  build-obg:
    name: "Build Optimal Budget Generator: Evidence-Based Budget Al"
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Cache Quarto freeze
        uses: actions/cache@v4
        with:
          path: _freeze
          key: quarto-freeze-obg-${{ hashFiles('**/*.qmd', '**/*.ipynb') }}

          restore-keys: |
            quarto-freeze-obg-
            quarto-freeze-

      - name: Set up Quarto
        uses: quarto-dev/quarto-actions/setup@v2

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true

      - name: Install Graphviz
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y graphviz

      - name: Install Python dependencies
        run: uv pip install --system -r requirements.txt

      - name: Install dih_models package
        run: uv pip install --system -e .

      - name: Install Jupyter kernel
        run: |
          python -m ipykernel install --user --name dih-project-kernel --display-name "DIH Project"

      - name: Cache TinyTeX
        uses: actions/cache@v4
        with:
          path: ~/.TinyTeX
          key: tinytex-${{ runner.os }}-${{ hashFiles('.github/workflows/publish.yml') }}
          restore-keys: |
            tinytex-${{ runner.os }}-

      - name: Install LaTeX
        run: |
          if [ ! -d ~/.TinyTeX ]; then
            quarto install tinytex
          fi
          ~/.TinyTeX/bin/*/tlmgr update --self || true
          ~/.TinyTeX/bin/*/tlmgr install fontspec unicode-math lualatex-math luaotfload || true

      - name: "Render Build Optimal Budget Generator: Evidence-Based Budget Al"
        run: python scripts/render-quarto.py obg
        timeout-minutes: 30

      - name: Upload LaTeX logs on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: latex-logs-obg
          path: |
            _build_temp/obg/*.log
            _build_temp/obg/**/*.log
            _build_temp/obg/*.aux
            _build_temp/obg/**/*.aux
            _build_temp/obg/*.toc
            _build_temp/obg/**/*.toc
            _build_temp/obg/*.out
            _build_temp/obg/**/*.out
          retention-days: 7


      - name: "Deploy Build Optimal Budget Generator: Evidence-Based Budget Al to Netlify (immediate)"
        if: github.event_name == 'push'
        uses: nwtgck/actions-netlify@v3.0
        with:
          publish-dir: '_build_temp/obg/_site/obg'
          production-branch: master
          github-token: ${{ secrets.GITHUB_TOKEN }}

          deploy-message: "Deploy obg site from GitHub Actions"
          enable-pull-request-comment: true
          enable-commit-comment: true
          overwrites-pull-request-comment: true
        env:
          NETLIFY_AUTH_TOKEN: ${{ secrets.NETLIFY_AUTH_TOKEN }}

          NETLIFY_SITE_ID: 123f44a0-d5b6-4ff3-9375-1f7e47fca053

        timeout-minutes: 1





  build-opg:
    name: "Build Optimal Policy Generator: Evidence-Based Policy Re"
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Cache Quarto freeze
        uses: actions/cache@v4
        with:
          path: _freeze
          key: quarto-freeze-opg-${{ hashFiles('**/*.qmd', '**/*.ipynb') }}

          restore-keys: |
            quarto-freeze-opg-
            quarto-freeze-

      - name: Set up Quarto
        uses: quarto-dev/quarto-actions/setup@v2

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true

      - name: Install Graphviz
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y graphviz

      - name: Install Python dependencies
        run: uv pip install --system -r requirements.txt

      - name: Install dih_models package
        run: uv pip install --system -e .

      - name: Install Jupyter kernel
        run: |
          python -m ipykernel install --user --name dih-project-kernel --display-name "DIH Project"

      - name: Cache TinyTeX
        uses: actions/cache@v4
        with:
          path: ~/.TinyTeX
          key: tinytex-${{ runner.os }}-${{ hashFiles('.github/workflows/publish.yml') }}
          restore-keys: |
            tinytex-${{ runner.os }}-

      - name: Install LaTeX
        run: |
          if [ ! -d ~/.TinyTeX ]; then
            quarto install tinytex
          fi
          ~/.TinyTeX/bin/*/tlmgr update --self || true
          ~/.TinyTeX/bin/*/tlmgr install fontspec unicode-math lualatex-math luaotfload || true

      - name: "Render Build Optimal Policy Generator: Evidence-Based Policy Re"
        run: python scripts/render-quarto.py opg
        timeout-minutes: 30

      - name: Upload LaTeX logs on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: latex-logs-opg
          path: |
            _build_temp/opg/*.log
            _build_temp/opg/**/*.log
            _build_temp/opg/*.aux
            _build_temp/opg/**/*.aux
            _build_temp/opg/*.toc
            _build_temp/opg/**/*.toc
            _build_temp/opg/*.out
            _build_temp/opg/**/*.out
          retention-days: 7


      - name: "Deploy Build Optimal Policy Generator: Evidence-Based Policy Re to Netlify (immediate)"
        if: github.event_name == 'push'
        uses: nwtgck/actions-netlify@v3.0
        with:
          publish-dir: '_build_temp/opg/_site/opg'
          production-branch: master
          github-token: ${{ secrets.GITHUB_TOKEN }}

          deploy-message: "Deploy opg site from GitHub Actions"
          enable-pull-request-comment: true
          enable-commit-comment: true
          overwrites-pull-request-comment: true
        env:
          NETLIFY_AUTH_TOKEN: ${{ secrets.NETLIFY_AUTH_TOKEN }}

          NETLIFY_SITE_ID: 8b9558d9-6a2b-4581-bde7-11d79933d105

        timeout-minutes: 1





  build-optimocracy:
    name: "Build Optimocracy: Evidence-Based Governance Through Out"
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Cache Quarto freeze
        uses: actions/cache@v4
        with:
          path: _freeze
          key: quarto-freeze-optimocracy-${{ hashFiles('**/*.qmd', '**/*.ipynb') }}

          restore-keys: |
            quarto-freeze-optimocracy-
            quarto-freeze-

      - name: Set up Quarto
        uses: quarto-dev/quarto-actions/setup@v2

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true

      - name: Install Graphviz
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y graphviz

      - name: Install Python dependencies
        run: uv pip install --system -r requirements.txt

      - name: Install dih_models package
        run: uv pip install --system -e .

      - name: Install Jupyter kernel
        run: |
          python -m ipykernel install --user --name dih-project-kernel --display-name "DIH Project"

      - name: Cache TinyTeX
        uses: actions/cache@v4
        with:
          path: ~/.TinyTeX
          key: tinytex-${{ runner.os }}-${{ hashFiles('.github/workflows/publish.yml') }}
          restore-keys: |
            tinytex-${{ runner.os }}-

      - name: Install LaTeX
        run: |
          if [ ! -d ~/.TinyTeX ]; then
            quarto install tinytex
          fi
          ~/.TinyTeX/bin/*/tlmgr update --self || true
          ~/.TinyTeX/bin/*/tlmgr install fontspec unicode-math lualatex-math luaotfload || true

      - name: "Render Build Optimocracy: Evidence-Based Governance Through Out"
        run: python scripts/render-quarto.py optimocracy
        timeout-minutes: 30

      - name: Upload LaTeX logs on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: latex-logs-optimocracy
          path: |
            _build_temp/optimocracy/*.log
            _build_temp/optimocracy/**/*.log
            _build_temp/optimocracy/*.aux
            _build_temp/optimocracy/**/*.aux
            _build_temp/optimocracy/*.toc
            _build_temp/optimocracy/**/*.toc
            _build_temp/optimocracy/*.out
            _build_temp/optimocracy/**/*.out
          retention-days: 7


      - name: "Deploy Build Optimocracy: Evidence-Based Governance Through Out to Netlify (immediate)"
        if: github.event_name == 'push'
        uses: nwtgck/actions-netlify@v3.0
        with:
          publish-dir: '_build_temp/optimocracy/_site/optimocracy'
          production-branch: master
          github-token: ${{ secrets.GITHUB_TOKEN }}

          deploy-message: "Deploy optimocracy site from GitHub Actions"
          enable-pull-request-comment: true
          enable-commit-comment: true
          overwrites-pull-request-comment: true
        env:
          NETLIFY_AUTH_TOKEN: ${{ secrets.NETLIFY_AUTH_TOKEN }}

          NETLIFY_SITE_ID: c6e535bc-4503-4db8-833a-c1b93f5e0f10

        timeout-minutes: 1





  build-political-dysfunction-tax:
    name: "Build The Political Dysfunction Tax"
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Cache Quarto freeze
        uses: actions/cache@v4
        with:
          path: _freeze
          key: quarto-freeze-political-dysfunction-tax-${{ hashFiles('**/*.qmd', '**/*.ipynb') }}

          restore-keys: |
            quarto-freeze-political-dysfunction-tax-
            quarto-freeze-

      - name: Set up Quarto
        uses: quarto-dev/quarto-actions/setup@v2

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true

      - name: Install Graphviz
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y graphviz

      - name: Install Python dependencies
        run: uv pip install --system -r requirements.txt

      - name: Install dih_models package
        run: uv pip install --system -e .

      - name: Install Jupyter kernel
        run: |
          python -m ipykernel install --user --name dih-project-kernel --display-name "DIH Project"

      - name: Cache TinyTeX
        uses: actions/cache@v4
        with:
          path: ~/.TinyTeX
          key: tinytex-${{ runner.os }}-${{ hashFiles('.github/workflows/publish.yml') }}
          restore-keys: |
            tinytex-${{ runner.os }}-

      - name: Install LaTeX
        run: |
          if [ ! -d ~/.TinyTeX ]; then
            quarto install tinytex
          fi
          ~/.TinyTeX/bin/*/tlmgr update --self || true
          ~/.TinyTeX/bin/*/tlmgr install fontspec unicode-math lualatex-math luaotfload || true

      - name: "Render Build The Political Dysfunction Tax"
        run: python scripts/render-quarto.py political-dysfunction-tax
        timeout-minutes: 30

      - name: Upload LaTeX logs on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: latex-logs-political-dysfunction-tax
          path: |
            _build_temp/political-dysfunction-tax/*.log
            _build_temp/political-dysfunction-tax/**/*.log
            _build_temp/political-dysfunction-tax/*.aux
            _build_temp/political-dysfunction-tax/**/*.aux
            _build_temp/political-dysfunction-tax/*.toc
            _build_temp/political-dysfunction-tax/**/*.toc
            _build_temp/political-dysfunction-tax/*.out
            _build_temp/political-dysfunction-tax/**/*.out
          retention-days: 7


      - name: "Deploy Build The Political Dysfunction Tax to Netlify (immediate)"
        if: github.event_name == 'push'
        uses: nwtgck/actions-netlify@v3.0
        with:
          publish-dir: '_build_temp/political-dysfunction-tax/_site/political-dysfunction-tax'
          production-branch: master
          github-token: ${{ secrets.GITHUB_TOKEN }}

          deploy-message: "Deploy political-dysfunction-tax site from GitHub Actions"
          enable-pull-request-comment: true
          enable-commit-comment: true
          overwrites-pull-request-comment: true
        env:
          NETLIFY_AUTH_TOKEN: ${{ secrets.NETLIFY_AUTH_TOKEN }}

          NETLIFY_SITE_ID: 1c74b376-7837-436e-b57b-8579b7a1939e

        timeout-minutes: 1





  build-wishocracy:
    name: "Build Wishocracy: Solving the Democratic Principal-Agent"
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Cache Quarto freeze
        uses: actions/cache@v4
        with:
          path: _freeze
          key: quarto-freeze-wishocracy-${{ hashFiles('**/*.qmd', '**/*.ipynb') }}

          restore-keys: |
            quarto-freeze-wishocracy-
            quarto-freeze-

      - name: Set up Quarto
        uses: quarto-dev/quarto-actions/setup@v2

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true

      - name: Install Graphviz
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y graphviz

      - name: Install Python dependencies
        run: uv pip install --system -r requirements.txt

      - name: Install dih_models package
        run: uv pip install --system -e .

      - name: Install Jupyter kernel
        run: |
          python -m ipykernel install --user --name dih-project-kernel --display-name "DIH Project"

      - name: Cache TinyTeX
        uses: actions/cache@v4
        with:
          path: ~/.TinyTeX
          key: tinytex-${{ runner.os }}-${{ hashFiles('.github/workflows/publish.yml') }}
          restore-keys: |
            tinytex-${{ runner.os }}-

      - name: Install LaTeX
        run: |
          if [ ! -d ~/.TinyTeX ]; then
            quarto install tinytex
          fi
          ~/.TinyTeX/bin/*/tlmgr update --self || true
          ~/.TinyTeX/bin/*/tlmgr install fontspec unicode-math lualatex-math luaotfload || true

      - name: "Render Build Wishocracy: Solving the Democratic Principal-Agent"
        run: python scripts/render-quarto.py wishocracy
        timeout-minutes: 30

      - name: Upload LaTeX logs on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: latex-logs-wishocracy
          path: |
            _build_temp/wishocracy/*.log
            _build_temp/wishocracy/**/*.log
            _build_temp/wishocracy/*.aux
            _build_temp/wishocracy/**/*.aux
            _build_temp/wishocracy/*.toc
            _build_temp/wishocracy/**/*.toc
            _build_temp/wishocracy/*.out
            _build_temp/wishocracy/**/*.out
          retention-days: 7


      - name: "Deploy Build Wishocracy: Solving the Democratic Principal-Agent to Netlify (immediate)"
        if: github.event_name == 'push'
        uses: nwtgck/actions-netlify@v3.0
        with:
          publish-dir: '_build_temp/wishocracy/_site/wishocracy'
          production-branch: master
          github-token: ${{ secrets.GITHUB_TOKEN }}

          deploy-message: "Deploy wishocracy site from GitHub Actions"
          enable-pull-request-comment: true
          enable-commit-comment: true
          overwrites-pull-request-comment: true
        env:
          NETLIFY_AUTH_TOKEN: ${{ secrets.NETLIFY_AUTH_TOKEN }}

          NETLIFY_SITE_ID: 6d3175fc-5866-4d09-985d-aebe008a4966

        timeout-minutes: 1





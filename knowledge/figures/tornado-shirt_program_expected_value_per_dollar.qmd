```{python}
#| echo: false

import matplotlib.pyplot as plt
import numpy as np
from pathlib import Path

from dih_models.plotting.chart_style import (
    setup_chart_style, add_watermark, clean_spines,
    COLOR_BLACK, COLOR_WHITE, add_png_metadata, get_figure_output_path,
    get_tick_formatter
)
from dih_models.parameters import format_parameter_value

setup_chart_style()

# Display name for chart title

display_name = "Shirt Program Expected Value per Dollar"

# Baseline and units

baseline = 423932755.0132492

# Tornado data from sensitivity analysis

drivers = ['SHIRT_SEED_WEARERS_THRESHOLD', 'SHIRT_SEED_COST_PER_WEARER_USD', 'SHIRT_CASCADE_PROBABILITY_GIVEN_SEED', 'NEW_DISEASE_FIRST_TREATMENTS_PER_YEAR', 'RARE_DISEASES_COUNT_GLOBAL', 'EVENTUALLY_AVOIDABLE_DALY_PCT', 'STANDARD_ECONOMIC_QALY_VALUE_USD', 'DFDA_PRAGMATIC_TRIAL_COST_PER_PATIENT', 'GLOBAL_ANNUAL_DALY_BURDEN', 'CURRENT_TRIAL_SLOTS_AVAILABLE', 'EFFICACY_LAG_YEARS', 'DFDA_OPEX_PLATFORM_MAINTENANCE', 'DFDA_OPEX_STAFF', 'DFDA_OPEX_INFRASTRUCTURE', 'DFDA_OPEX_REGULATORY', 'DFDA_OPEX_COMMUNITY']
driver_labels = ['Shirt Seed Wearers Threshold', 'Shirt Seed Cost per Wearer', 'Shirt Cascade Probability Given Seed', 'Diseases Getting First Treatment Per Year', 'Total Number of Rare Diseases Globally', 'Eventually Avoidable DALY Percentage', 'Standard Economic Value per QALY', 'Pragmatic Trial Cost per Patient', 'Global Annual DALY Burden', 'Annual Global Clinical Trial Participants', 'Regulatory Delay for Efficacy Testing Post-Safety Verification', 'Pragmatic Trial Platform Maintenance Costs', 'Pragmatic Trial Platform Staff Costs', 'Pragmatic Trial Platform Infrastructure Costs', 'Pragmatic Trial Platform Regulatory Coordination Costs', 'Pragmatic Trial Platform Community Support Costs']
impacts_low = [3815394795.1192427, 1695731020.0529969, -339146204.0105994, 356585702.96811897, -58218073.9539786, -195096099.6823669, -84786551.00264984, 32219111.83077407, -22079830.990273356, 7573774.010827184, -4001521.3013167977, 8264.513335883617, 4959.163702368736, 4959.163702368736, 3306.2610560655594, 1653.2064955830574]
impacts_high = [-339146204.0105994, -317949566.2599369, 593505857.018549, -203763258.83892515, 174654221.8619359, 24587089.435280144, 84786551.00264984, -80199255.530689, 22079830.990273416, -7573774.010827243, 4001521.3013167977, -11576.701414108276, -8268.312238872051, -6614.345753192902, -4960.531307399273, -1653.358451783657]

# Convert deltas to absolute values (baseline + delta)

values_low = [baseline + delta for delta in impacts_low]
values_high = [baseline + delta for delta in impacts_high]

# Create tornado chart (horizontal bars showing swing range)
# Cap dimensions to avoid LaTeX "Dimension too large" errors in PDF output
# 8x10 inches max keeps figures compact and PDF-compatible

fig, ax = plt.subplots(figsize=(8, min(10, max(5, len(drivers) * 0.6))))

y_pos = np.arange(len(drivers))

# Plot low impact (left side)

for i, (low, high) in enumerate(zip(values_low, values_high)):
    left = min(low, high)
    width_low = baseline - left if left < baseline else 0
    width_high = max(low, high) - baseline if max(low, high) > baseline else 0

    # White bar for range below baseline

    if width_low > 0:
        ax.barh(i, width_low, left=left,
                color=COLOR_WHITE, edgecolor=COLOR_BLACK, linewidth=2)

    # Black bar for range above baseline

    if width_high > 0:
        ax.barh(i, width_high, left=baseline,
                color=COLOR_BLACK, edgecolor=COLOR_BLACK, linewidth=2)

# Format axis

ax.set_yticks(y_pos)
# Simplified labels (just parameter names)

ax.set_yticklabels(driver_labels, fontsize=11)
ax.set_title(f'Sensitivity Analysis: {display_name}', fontsize=16, weight='bold', pad=20)

# X-axis label with units

units_label = "ratio"
if units_label:
    ax.set_xlabel(f'{display_name} ({units_label})', fontsize=12)
else:
    ax.set_xlabel(f'{display_name}', fontsize=12)

# Format x-axis tick labels with K/M/B/T notation

ax.xaxis.set_major_formatter(get_tick_formatter(unit=units_label))

# Add vertical line at baseline

ax.axvline(baseline, color=COLOR_BLACK, linewidth=1, linestyle='--', alpha=0.5)

# Clean spines

clean_spines(ax)

# Add watermark

add_watermark(fig)

# Save PNG to knowledge/figures/ regardless of where Quarto renders from
# Use 96 DPI for web-optimized output (reduced from 150 DPI to minimize file size)

output_path = get_figure_output_path('tornado-shirt_program_expected_value_per_dollar.png')
plt.savefig(output_path, dpi=96, bbox_inches=None, facecolor=COLOR_WHITE)

add_png_metadata(
    output_path,
    title=f'Sensitivity: {display_name}',
    description=f'Tornado diagram showing which input parameters have the largest impact on {display_name}'
)

plt.show()
```
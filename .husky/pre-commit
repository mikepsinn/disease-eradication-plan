#!/usr/bin/env sh
set -eu

if [ -f ".venv/Scripts/python.exe" ]; then
    PYTHON_CMD=".venv/Scripts/python.exe"
elif [ -f ".venv/bin/python" ]; then
    PYTHON_CMD=".venv/bin/python"
else
    PYTHON_CMD="python"
fi

START_TIME=$(date +%s)

run_pnpm() {
    if command -v pnpm >/dev/null 2>&1; then
        pnpm "$@"
    elif command -v corepack >/dev/null 2>&1; then
        corepack pnpm "$@"
    else
        echo "pnpm is required. Install Node.js 22 and enable Corepack."
        return 1
    fi
}

echo "Running Python type check..."
if ! run_pnpm exec pyright dih_models scripts; then
    echo "Python type check failed. Fix Pyright/Pylance errors before committing."
    exit 1
fi

echo "Running pre-render validation (including artifact generation)..."
if ! "$PYTHON_CMD" -u scripts/pre-render-validation.py; then
    echo "Pre-render validation failed. Fix the errors before committing."
    exit 1
fi

END_TIME=$(date +%s)
echo "Pre-commit checks passed in $((END_TIME - START_TIME)) seconds."
